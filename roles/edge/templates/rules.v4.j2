*filter
:INPUT DROP [0:0]
:FORWARD ACCEPT [0:0]
:OUTPUT ACCEPT [0:0]

# 1. Allow Loopback interface
-A INPUT -i lo -j ACCEPT

# 2. Allow Established and Related connections
-A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT

# 3. Allow ICMP (Ping)
-A INPUT -p icmp -j ACCEPT

# 4. Allow SSH (Public - Port 22)
-A INPUT -p tcp --dport 22 -j ACCEPT

# 5. Allow Public Web Traffic (Global)
-A INPUT -p tcp --dport 80 -j ACCEPT
-A INPUT -p tcp --dport 443 -j ACCEPT

# 6. Allow Xray (Dynamic: Applies if xray_port_tcp is defined)
{% if xray_port_tcp is defined %}
-A INPUT -p tcp --dport {{ xray_port_tcp }} -j ACCEPT
{% endif %}

COMMIT

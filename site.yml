---
# =============================================================================
# 1. EDGE NODES (Standalone Bastions/Proxies)
# =============================================================================
- name: Configure Edge Nodes
  hosts: edge_nodes
  become: true
  gather_facts: true
  roles:
    - edge

# =============================================================================
# 2. CONTROL PLANE (Install K3s Server & Cilium)
# =============================================================================
- name: Configure Control Plane
  hosts: prod_control_plane:lab_control_plane
  become: true
  gather_facts: true
  roles:
    - control

# =============================================================================
# 3. WORKER NODES (Join Cluster)
# =============================================================================
- name: Configure Worker Nodes
  hosts: prod_worker_nodes:lab_worker_nodes
  become: true
  gather_facts: true
  roles:
    - worker
